// Создаем div элемент
const div = document.createElement("div");

// Добавляем класс к div элементу
div.classList.add("triangle");

// Добавляем текст в div элемент
div.textContent = "Привет, мир!";

// Добавляем div элемент в тело документа
document.body.appendChild(div);
